#include<bits/stdc++.h>
using namespace std;

template<class G, class T> 
pair<G, T> operator + (pair<G, T> a, pair<G, T> b) { return make_pair(a.first + b.first, a.second + b.second); } 

#define f first
#define s second
#define pb push_back
#define sz(s) (int)s.size()
#define all(s) s.begin(), s.end()
#define mp make_pair

typedef long long ll;

void solve(int test){
    return;
}

int main(){
    int testy = 1;
    // cin >> testy;

    for(int i = 1; i <= testy; i++){
        solve(i);
    }
    return 0;
}